import styled from 'styled-components';
import Button from '../styles/Button';


const Main = () => {
  return(
    <main>
      <p>main part</p>
    </main>
  );
};

export default Main;
